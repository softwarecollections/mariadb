diff -up mariadb-5.5.33a/mariadb.service.scl-env-check mariadb-5.5.33a/mariadb.service
--- mariadb-5.5.33a/mariadb.service.scl-env-check	2013-11-22 16:08:26.985359830 +0100
+++ mariadb-5.5.33a/mariadb.service	2013-11-22 16:17:17.190877892 +0100
@@ -27,11 +27,16 @@ Type=simple
 User=mysql
 Group=mysql
 
-ExecStartPre=/usr/libexec/mysqld-prepare-db-dir %n
+# Load collections set to enabled for this service
+EnvironmentFile=__SCL_SCRIPTS__/service-environment
+
+# We want to start server only inside "scl enable" invocation
+ExecStartPre=/usr/bin/scl enable $MARIADB55_SCLS_ENABLED -- /usr/bin/scl_enabled __SCL_NAME__
+ExecStartPre=/usr/bin/scl enable $MARIADB55_SCLS_ENABLED -- /usr/libexec/mysqld-prepare-db-dir %n
 # Note: we set --basedir to prevent probes that might trigger SELinux alarms,
 # per bug #547485
-ExecStart=/usr/bin/mysqld_safe --basedir=/usr
-ExecStartPost=/usr/libexec/mysqld-wait-ready $MAINPID
+ExecStart=/usr/bin/scl enable $MARIADB55_SCLS_ENABLED -- /usr/bin/mysqld_safe --basedir=/usr
+ExecStartPost=/usr/bin/scl enable $MARIADB55_SCLS_ENABLED -- /usr/libexec/mysqld-wait-ready $MAINPID
 
 # Give a reasonable amount of time for the server to start up/shut down
 TimeoutSec=300
diff -up mariadb-5.5.33a/scripts/mysqld_safe.sh.scl-env-check mariadb-5.5.33a/scripts/mysqld_safe.sh
--- mariadb-5.5.33a/scripts/mysqld_safe.sh.scl-env-check	2013-09-20 00:34:22.000000000 +0200
+++ mariadb-5.5.33a/scripts/mysqld_safe.sh	2013-11-22 16:15:48.564769828 +0100
@@ -11,6 +11,12 @@
 # mysql.server works by first doing a cd to the base directory and from there
 # executing mysqld_safe
 
+# we want start daemon only inside "scl enable" invocation
+if ! scl_enabled __SCL_NAME__ ; then
+    echo "Use \"scl enable __SCL_NAME__ 'service ...'\" invocation"
+    exit 1
+fi
+
 # Initialize script globals
 KILL_MYSQLD=1;
 MYSQLD=
diff -up mariadb-5.5.33a/scripts/mysql_install_db.sh.scl-env-check mariadb-5.5.33a/scripts/mysql_install_db.sh
--- mariadb-5.5.33a/scripts/mysql_install_db.sh.scl-env-check	2013-09-20 00:34:24.000000000 +0200
+++ mariadb-5.5.33a/scripts/mysql_install_db.sh	2013-11-22 16:15:48.565769829 +0100
@@ -19,6 +19,12 @@
 #
 # All unrecognized arguments to this script are passed to mysqld.
 
+# we want start daemon only inside "scl enable" invocation
+if ! scl_enabled __SCL_NAME__ ; then
+    echo "Use \"scl enable __SCL_NAME__ 'service ...'\" invocation"
+    exit 1
+fi
+
 basedir=""
 builddir=""
 ldata="@localstatedir@"
